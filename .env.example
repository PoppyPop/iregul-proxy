# iRegul Proxy Configuration
# Copy this file to .env and modify the values as needed

# Proxy server settings
PROXY_HOST=0.0.0.0
PROXY_PORT=65001

# Upstream server settings (where to forward heat pump data)
UPSTREAM_HOST=vpn.i-regul.com
UPSTREAM_PORT=65001

# API server settings
API_HOST=0.0.0.0
API_PORT=8080
